{
  "name": "Resume LaTeX Devcontainer",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "settings": {},
  "extensions": [
    "James-Yu.latex-workshop",
    "yzhang.markdown-all-in-one",
    "GitHub.copilot"
  ],
  "postCreateCommand": "cd backend && uv sync --extra dev && cd ../frontend && npm install",
  "remoteUser": "vscode"
}
