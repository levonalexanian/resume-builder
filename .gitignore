# Secrets and local overrides (copy from .env.example)
.env

# Generated pipeline artifacts (per-user, on the filesystem)
users/*/resumes/

# Prisma CLI dependencies (installed at the repo root)
/node_modules

# Working artifact from the Alembic→Prisma DDL diff check
/alembic_baseline.sql
